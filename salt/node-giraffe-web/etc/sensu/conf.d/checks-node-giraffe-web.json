{
  "checks": {
    "check-http-node-giraffe-web": {
      "command"     : "/etc/sensu/plugins/http/check-http.rb -h 'localhost' -P '9000' -p '/'",
      "handlers"    : ["default"],
      "interval"    :   60,
      "standalone"  :   true
    }
  }
}
