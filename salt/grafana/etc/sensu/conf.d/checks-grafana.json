{
  "checks": {
    "check-http-grafana": {
      "command"     : "/etc/sensu/plugins/http/check-http.rb -h 'localhost' -P '8000' -p '/'",
      "handlers"    : ["default"],
      "interval"    :   60,
      "standalone"  :   true
    }
  }
}
